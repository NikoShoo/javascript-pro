# Конспект JavaScript Pro


## Тема 1: Ошибки в JS
В *JavaScript* есть встроенный объект Error

### Свойства объекта Error:
* **name** — тип ошибки (например, `TypeError`)
* **message** — что пошло не так
* **stack** — вся цепочка вызовов
